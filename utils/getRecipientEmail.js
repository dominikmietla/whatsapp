
const getRecipientEmail = (users, userLoggedIn) => {
    let userName = '';

    users.forEach(user => {
            userName = user;
    });

    return userName;

}
export default getRecipientEmail;